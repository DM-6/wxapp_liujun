<view class="container">
    <map id="ofoMap" longitude="{{longitude}}" latitude="{{latitude}}" scale="{{scale}}" 
         controls="{{controls}}"/>

    <!-- UI组件（微信内置组件） 一组标签？map 是原生的html提供不了的
    html标签在诞生时，展示文档，简单就好，能力有限
    提供了一个牛逼的功能，封装起来，用一个新的标签来表示
    功能极其强大 -->
</view>
